{
  "name": "database",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "cargo build",
    "build": "cargo build --release",
    "start": "cargo build --release"
  },
  "dependencies": {
    "grpc-proto": "*"
  }
}
