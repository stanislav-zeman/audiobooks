{
  "name": "backend",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "RUST_LOG=tower_http=trace cargo watch -x run || cargo watch -x run",
    "build": "cargo build --release",
    "start": "cargo run --release"
  },
  "dependencies": {
    "grpc-proto": "*"
  }
}
